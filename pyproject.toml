[build-system]
requires = [
    "setuptools<82",  # v82 removed pkg_resources, needed by farm-ng-package
    "pybind11",
    "wheel",
    "farm-ng-package",
    "protobuf>=5.26.0",
    "grpcio-tools>=1.76.0",
]
build-backend = "setuptools.build_meta"
