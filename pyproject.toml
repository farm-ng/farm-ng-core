[build-system]
requires = [
    "setuptools",
    "pybind11",
    "wheel",
    # Note: v0.1.4 was yanked.
    "farm-ng-package",
    "protobuf",
    "grpcio-tools",
]
build-backend = "setuptools.build_meta"
