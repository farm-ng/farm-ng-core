name: Continuous Integration [python]

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  py-cpu:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        # Using macos-13 bc they removed support for python 3.8 & 3.9 on macos-14 (now macos-latest)
        # See: https://github.com/actions/setup-python/issues/696#issuecomment-2071769156
        os: [ubuntu-latest, macos-12, macos-13, macos-latest]
        python-version: ['3.8', '3.9', '3.10', '3.11', '3.12']
    steps:
    - uses: actions/checkout@v3
      with:
        submodules: "recursive"
    - uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install dependencies
      run: pip3 install -vvv -e .[dev]
    - name: Run sequential tests
      run: ./run_python_tests.sh
    - name: Run asyncio tests
      run: ./run_python_tests.sh --asyncio
    - name: Run mypy
      run: mypy py/farm_ng
