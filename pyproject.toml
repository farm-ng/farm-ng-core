[build-system]
requires = [
    "setuptools",
    "pybind11",
    "wheel",
    # Note: v0.1.4 was yanked.
    "farm-ng-package>=0.1.3",
    "grpcio-tools==1.64.1",
]
build-backend = "setuptools.build_meta"
